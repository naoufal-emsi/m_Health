<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.M_Health">
        <activity
            android:name="m.mhealth.ui.patient.HomeMedecin"
            android:exported="false" />
        <!-- ÉCRAN PATIENT -->
        <activity
            android:name="m.mhealth.ui.patient.PatientActivity"
            android:exported="true" /> <!-- ÉCRAN BOOK APPOINTMENT -->
        <activity
            android:name="m.mhealth.ui.patient.BookAppointmentActivity"
            android:exported="true" /> <!-- LOGIN (LAUNCHER) -->
        <activity
            android:name=".AdminLoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".UserManagementActivity" />
        <activity android:name=".PatientDatabaseActivity" />
        <activity android:name=".SystemMonitorActivity" />
    </application>

</manifest>